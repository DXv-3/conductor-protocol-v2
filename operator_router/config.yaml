# operator_router/config.yaml
route_on_pass: canonical
route_on_fail: blocked_queue

# Path to the-brain repo root (optional: overrides BRAIN_REPO_PATH env var)
# brain_db_path: /absolute/path/to/the-brain/brain.db
